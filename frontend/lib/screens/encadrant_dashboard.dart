import 'package:flutter/material.dart';

class EncadrantDashboard extends StatelessWidget {
  const EncadrantDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Encadrant Dashboard')),
    );
  }
}